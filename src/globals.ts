export const CONCURRENCY = Number(process.env.CONCURRENCY || 1);
export const BULK = Number(process.env.BULK || 1);
export const COUNT = Number(process.env.COUNT || 1);
export const VERBOSE = Boolean(process.env.VERBOSE || 0);
